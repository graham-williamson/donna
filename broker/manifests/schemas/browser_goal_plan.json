{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "donna://schemas/browser_goal_plan.json",
  "title": "browser_goal.plan params",
  "type": "object",
  "additionalProperties": false,
  "required": ["site", "goal", "phase"],
  "properties": {
    "site": {
      "type": "string",
      "description": "Site profile key (e.g. 'everyone_active')"
    },
    "goal": {
      "type": "string",
      "description": "Natural-language description of the goal to plan for"
    },
    "phase": {
      "type": "string",
      "const": "plan",
      "description": "Must be 'plan' — read-only planning pass, no side-effects"
    },
    "max_actions": {
      "type": "integer",
      "description": "Optional cap on the number of browser actions the planner may take"
    }
  }
}
