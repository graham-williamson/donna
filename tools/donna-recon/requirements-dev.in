-r requirements.in

# Dev/test toolchain. Compile with:
#   pip-compile --generate-hashes --output-file=requirements-dev.txt requirements-dev.in

pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
mypy>=1.11,<2.0

# pip-tools so pip-compile is available in the locked venv.
pip-tools>=7.4,<8.0
